apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: eg
  labels:
    app.kubernetes.io/name: envoy-gateway
    app.kubernetes.io/part-of: kanboard-platform
spec:
  controllerName: gateway.envoyproxy.io/gatewayclass-controller
